apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Helm-based addons (HelmRepository + ModuleTemplate + Module)
resources:
  - gpu-operator/
  - hami/
  - prometheus/
  - istio/
  - llm-d-infra/
  - module-templates/
