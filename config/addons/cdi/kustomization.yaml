apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# CDI — ModuleTemplate + kustomization pattern (like prometheus)
# Deploy order: GitRepository first, then ModuleTemplate, then Module
resources:
  - moduletemplate.yaml
  - module.yaml
