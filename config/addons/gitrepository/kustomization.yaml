apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# GitRepository only. ModuleTemplates come from build-installer (config/addons/gitrepository/module-templates).
resources:
  - gitrepository.yaml
