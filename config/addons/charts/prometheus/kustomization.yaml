apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Deploy order matters: source must exist before the HelmRelease is created,
# and the ModuleTemplate must exist before the Module references it.
# Resources are applied in the order listed below.
resources:
  - helmrepository.yaml
  - moduletemplate.yaml
  - module.yaml
